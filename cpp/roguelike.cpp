#include "stdio"
#include "stdlib"
#include "iostream"
#include "math"

int main (int argc, char* argv[]) {
	// output string
	char* word;
	// enumerates through every element
	for (int i = 0; i < (argv.length / char); i ++) {
		word[i] = sin(argv[i]);
	}
	cout <<  word << endl;
	return 0;
}
